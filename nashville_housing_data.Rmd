---
title: "Exploring the Nashville Residential Parcel Transaction Dataset (2006–2022)"
author: "Nicc Forster-Benson"
date: "`r Sys.Date()`"
output: pdf_document
---


```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE, warning = FALSE, message = FALSE)

# Load libraries
library(tidyverse)
library(lubridate)
```


## Load the Dataset


```{r}
# Adjust path to where your dataset is stored
data <- read_csv("nashville_housing_data.csv")

```

##Data Cleaning

```{r}

# Ensure dates and numeric fields are parsed properly
data <- data %>%
  mutate(
    SaleDate = mdy(SaleDate),
    Year = year(SaleDate),
    SalePrice = as.numeric(SalePrice),
    Acres = as.numeric(Acres),
    FinishArea = as.numeric(FinishArea)
  )

summary(data$SalePrice)
```

##Sale Price Over Time

```{r}
data %>%
  group_by(Year) %>%
  summarise(MedianPrice = median(SalePrice, na.rm = TRUE)) %>%
  ggplot(aes(x = Year, y = MedianPrice)) +
  geom_line() +
  geom_point() +
  labs(
    title = "Median Residential Sale Price in Nashville (2006–2022)",
    x = "Year",
    y = "Median Sale Price (USD)"
  ) +
  scale_y_continuous(labels = scales::dollar)
```

##Distribution of Sale Prices


```{r}
data %>%
  filter(SalePrice > 1000, SalePrice < 2000000) %>%  # filter extreme outliers
  ggplot(aes(x = SalePrice)) +
  geom_histogram(bins = 50, fill = "steelblue", color = "white") +
  labs(
    title = "Distribution of Sale Prices",
    x = "Sale Price (USD)",
    y = "Count"
  ) +
  scale_x_continuous(labels = scales::dollar)
```

  
  
  
